# Netlify config — the app lives in ./game
[build]
  base    = "game"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so any path serves index.html
[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
